selecionar um modelo utilizado anteriormente ( exemplo - modelo de predição ) > realizar uma implementação funcional dentro de cliente federado que irá utilizar os métodos e funções do modelo escolhido > excluir o cliente honesto ( classe )pois não há necessidade  > e alterar a classe malicioso ( ajustando de acordo com o modelo selecionado - envenenar modelo = pesos e envenenar dados = dataframe do pandas)